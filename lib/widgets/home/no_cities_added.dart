import 'package:flutter/material.dart';

class NoCitiesAddedScreen extends StatelessWidget {
  const NoCitiesAddedScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Column(
      children: [Container(), Text('Welcome to Very ')],
    );
  }
}
